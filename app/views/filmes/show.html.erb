

<div class="segura-conteudo" style="padding: 0 !important;">
  
  <div class="conteudo internas">
    
    <div class="colLeft">
      <h1 class="titulo cinema">
        <span>cinema</span>
        <span class="sub-titulo"><%= @filme.titulo%></span>
      </h1>
      <div class="border-divisa-head clear"></div>
      <div class="compartilhar">
        <div class="segura-btn-share left">
          <!-- AddThis Button BEGIN -->
          <div class="addthis_toolbox addthis_default_style ">
            <a class="addthis_button_facebook_like" fb:like:layout="button_count"></a>
            <a class="addthis_button_tweet" style="width: 90px"></a>        
            <a class="addthis_counter addthis_pill_style"></a>
          </div>
          <script type="text/javascript" src="http://s7.addthis.com/js/250/addthis_widget.js#pubid=xa-501844af04cf4b7e"></script>
          <!-- AddThis Button END -->       
        </div>
        <div class="presenca right">
          
          <a href="/aumentar/<%= @filme.id%>" data-remote="true" class="btnpresenca">
            <span>Confirme sua presença</span>
          </a>
          <div class="counter-presenca">
            <span class="<%=@filme.id%>"><%= @filme.presenca%></span>

          </div>
        </div>
        <div class="clear"></div>
      </div>
      
      <%= youtube_embed(@filme.trailer) %>
      
      <ul class="dados-do-filme">
        <li class="head salas">
          <ul>
            <h2>Salas:</h2>
            <% @filme.salas.each do |s|%>
              <% if s.nome != ""%>
                <li><%= s.nome %>(
                  <%if s.legendado%>
                    <%= "Legendado"%>
                    <%else%>
                    <%= "Dublado"%>
                  <%end%>
                  )</li>
              <%end%>
            <%end%>
          </ul>
        </li>
        <li class="head tipo">
          <ul>
            <h2>Tipo:</h2>
           <%@filme.salas.each do |s| %>
              <% if s.nome != ""%>
                <%if s.tresD%>
                    <li><%= "3D"%></li>
                    <%else%>
                    <li><%= "2D"%></li>
                    
                <%end%>
              <%end%>
            <%end%>
            
          </ul>
        </li>
        <li class="head genero">
          <ul>
            <h2>Gênero</h2>
            <li><%= @filme.genero %></li>
          </ul>
        </li>
        <li class="head classificacao">
          <ul>
            <h2>Classificação:</h2>
            <li><%= @filme.classificacao %></li>
          </ul>
        </li>       
      </ul>
      
    </div>
    
    <div class="colRight">
      
      
      <div class="ingressos">
        <script type="text/javascript">
          $(document).ready(function(){
            $('.cinema-precos .box').bxSlider({
              controls: false,
              auto: true,
              speed: 200,
              mode: 'vertical'                        
            });
            
            $('.ingressos .ctn .titulos').bxSlider({
              controls: false,
              auto: true,
              speed: 600,
              mode: 'fade',
              easing: 'easeInOutBack'           
            });         
            
          });
        </script>
        <img src="/assets/top-ingresso-colunright.png" />
        <div class="ctn">
          <div class="titulos">
            <% @ingressos.each do |i|%>
            <h2 class="titulo"><%=i.nome %></h2>
            <%end%>
          </div>
          <ul class="cinema-precos">
            <div class="box">
              <% @ingressos.each do |i|%>
              <li>
                <span>
                  <h2>Inteira</h2>
                  <h3><%= i.inteira %></h3>                 
                </span>
                <span>
                  <h2>Meia</h2>
                  <h3><%= i.meia%></h3>
                </span>
                <div class="clear"></div>                         
              </li>

              <%end%>
             
            </div>
          </ul>         
          <a href="javascript:;" class="link">Veja a tabela de preços</a>
        </div>                
        <img src="/assets/bot-ingresso-colunright.png" />       
      </div>
      
      <div id="maisvotas">
        <h2 class="right-collun-title">
          Os mais votados
        </h2>
        
        <img src="/assets/categorias-block-top.png" />
        <div class="block-right-color">
         <%@filmes.each do |f|%>
          <dl class="maisvotados">
            <dt><span><%= f.presenca %></span></dt>
            <dd><a href="<%= filme_path(f)%>"><%= f.titulo%></a></dd>
          </dl>
          <%end%>
                
        </div>
        <img src="/assets/categorias-block-bot.png" />
      </div>    
      
    </div>
    
    <div class="clear"></div>
    
    
    <div class="segura-secoes">
      <img src="/assets/secao-bg-top.png" />
      <ul class="list-secoes">
        <li class="titulos">
          <span class="salas">Salas</span>
          <span class="seg-qui">Segunda à Quinta</span>
          <span class="sex-dom">Sexta, Sábado, Domingo e Feriádos</span>
          <span class="prox">Próxima Sessão</span>        
        </li>
<% @filme.salas.each do |s|%>
  <%if s.nome != ""%>
        <li class="hsala">
          <span class="salas"><%= s.nome %></span>
          <span class="seg-qui">
            <dl>
              
                <% if s.fh1 != "" %>
                <dt><%= s.fh1 %></dt>
                <%end%>

              
              
                <% if s.fh2 != "" %>
                <dt><%= s.fh2 %></dt>
                <%end%>

              
              
                <% if s.fh3 != "" %>
                <dt><%= s.fh3 %></dt>
                <%end%>

              
              
                <% if s.fh4 != "" %>
                <dt><%= s.fh4 %></dt>
                <%end%>

              
              
                <% if s.fh5 != "" %>
                <dt><%= s.fh5 %></dt>
                <%end%>

              
            </dl>
          </span>
          <span class="sex-dom">
            <dl>
              <% if s.sh1 != "" %>
                <dt><%= s.sh1 %></dt>
              <%end%>

              <% if s.sh2 != "" %>
                <dt><%= s.sh2 %></dt>
              <%end%>

              <% if s.sh3 != "" %>
                <dt><%= s.sh3 %></dt>
              <%end%>

              <% if s.sh4 != "" %>
                <dt><%= s.sh4 %></dt>
              <%end%>

              <% if s.sh5 != "" %>
                <dt><%= s.sh5 %></dt>
              <%end%>

              <!--
              <dt class="active">
                <span>
                  21:30
                </span>
              </dt>
              -->
            </dl>           
          </span>
          <span class="prox">
            <a class="proximas-secao" href="javascript:;">
              <span class="ctn"></span>
            </a>
          </span>         
        </li>
  <%end%>
<%end%>
        
              
        
      </ul>
      <img src="/assets/secao-bg-bot.png" />
      <div class="clear"></div>
    </div>
    
    
    <div class="colLeft" style="margin-top:0px;">
      <h2 class="titulo marcador">SINOPSE</h2>
      <div class="border-divisa-head"></div>
      <p><%= @filme.sinopse %></p>
      
      <h2 class="titulo marcador">ELENCO</h2>
      <div class="border-divisa-head"></div>
      
      <ul class="elenco-list">
        <p><%= @filme.elenco %></p>
        <div class="clear"></div>     
      </ul> 
                        
                        <div class="mdest-page">
                            <div class="form-title">Comentários</div>
                            
                            <ul class="cmm-list">
                              <%@filme.comentario_filmes.each do |c|%>
                                <%if c.autorizado %>
                                <li>
                                    <div class="rrow clearfix">
                                        <div class="avatar"><img src="/assets/avatar-ex.png" /></div>
                                        <div class="cmm">
                                            <div class="cmm-name"><%=c.nome%></div>
                                            <div class="cmm-text"><%=c.comentario%></div>
                                        </div>
                                    </div>
                                    <div class="share-respond">
                                        <a href="" title="">Curtir</a>
                                        <a href="" title="">Responder</a>
                                    </div>
                                </li>
                                <%end%>
                              <%end%>
                            </ul>
                            
                            <div class="form-title2">Deixe seu comentário</div>
                            <form method="post" action="/filmes/<%= @filme.id%>/comentario_filmes" id="lazer-form">
                                <div class="top-bg"></div>
                                <div class="middle-bg">
                                    <div class="row row-1 clearfix">
                                        <div class="left">
                                            <input type="text" name="comentario_filme[nome]" id="" placeholder="Digite aqui o seu nome:" /><br />
                                            <input type="text" name="comentario_filme[email]" id="" placeholder="Digite aqui o seu e-mail:" />
                                        </div>
                                        <div class="right"><textarea name="comentario_filme[comentario]" id="" placeholder="Digite aqui o seu comentário:"></textarea></div>
                                    </div>
                                    <div class="submit-btn"><button type="submit"></button></div>
                                </div>
                                <div class="bottom-bg"></div>
                            </form>
                        </div>
      <div class="clear"></div>
      
    </div>
    
    <div class="colRight" style="margin-top:0px;">
      
                            <div>
                                <h2 class="right-collun-title" style="margin-top: 10px">Filmes em cartaz</h2>
                                <img src="/assets/categorias-block-top.png" />
                                <div class="block-right-color" style="padding: 0 10px">
                                    <%= render 'filmes/slide'%>
                                </div>
                                <img src="/assets/categorias-block-bot.png" />
                            </div>
      
      <div>
        <h2 class="right-collun-title" style="margin-top: 10px">
          Se ligue nas promoções
        </h2>
        
        <img src="/assets/seliga-promo-top.png" />
        <form class="seliga-promo">
          <p>
            Cadastre-se e receba informes, noticias e fica por dentro e participe das nossas promoções.
          </p>
          <label>
            <input type="text" placeholder="Digite aqui o seu nome:" />
          </label>
          <label>
            <input type="text" placeholder="Digite aqui o seu e-mail:" />
          </label>
          <label style="margin: 0;">
            <a class="btn-secundary right">
              <span>Enviar</span>
            </a>
            <div class="clear"></div>
          </label>
        </form>
        <img src="/assets/seliga-promo-bot.png" />
      </div>  
      
      
    </div>
      
  </div>    
      
  <div class="clear"></div>     
</div>
  


<script type="text/javascript">
function formatTime(time) {
    var result = false, m;
    var re = /^\s*([01]?\d|2[0-3]):?([0-5]\d)\s*$/;
    if ((m = time.match(re))) {
        result = (m[1].length == 2 ? "" : "0") + m[1] + ":" + m[2];
    }
    return result;
}

function subtraiHora(hrA, hrB) {
            hrA = formatTime(hrA);
            hrB = formatTime(hrB);
            if(hrA.length != 5 || hrB.length != 5) return "00:00";
           
            temp = 0;
            nova_h = 0;
            novo_m = 0;
     
            hora1 = hrA.substr(0, 2) * 1;
            hora2 = hrB.substr(0, 2) * 1;
            minu1 = hrA.substr(3, 2) * 1;
            minu2 = hrB.substr(3, 2) * 1;
           
            temp = minu1-minu2;
            while(temp < 0) {
                    nova_h++;
                    temp = temp + 60;
            }
            novo_m = temp.toString().length == 2 ? temp : ("0" + temp);
     
            temp = hora1 - hora2 - nova_h;
            while(temp < 0) {
                    temp = temp + 24;
            }
            nova_h = temp.toString().length == 2 ? temp : ("0" + temp);
     
            return nova_h + ':' + novo_m;
 };

 

var dataAtual = new Date();

 if (dataAtual.getMinutes()<10){
  var hAtual=+dataAtual.getHours()+":0"+dataAtual.getMinutes();
 }
 else
 {
  var hAtual=dataAtual.getHours()+":"+dataAtual.getMinutes();
 }
 
 var dia = dataAtual.getDay();
 var fds = false;
 if((dia>4) || (dia<1)){
  fds = true;
 }
 else{
  fds = false;
  }


var $hsemana = [];
$('.hsala').each(function(sala){
  $hsemana[sala] = [];
    $('.hsala:eq('+sala+') .seg-qui dt').each(function(hora){
      $hsemana[sala][hora] = $(this)
  });
});
var $hfsemana = [];
$('.hsala').each(function(sala){
  $hfsemana[sala] = [];
    $('.hsala:eq('+sala+') .sex-dom dt').each(function(hora){
      $hfsemana[sala][hora] = $(this)
  });
});

var menorDs = [];
for (var x = 0 ; x < $hsemana.length ; x++) {
  for (var y = 0; y< $hsemana[x].length; y++){
    if (y <1){
      menorDs[x] = $hsemana[x][y];
    }
    var v1 = subtraiHora(menorDs[x].text(),hAtual);
    var v2 = subtraiHora($hsemana[x][y].text(),hAtual);

    if(v1 > v2){
      menorDs[x] = $hsemana[x][y]
    };

  };
};

var menorFds = [];
for (var x = 0 ; x < $hfsemana.length ; x++) {
  for (var y = 0; y< $hfsemana[x].length; y++){
    if (y <1){
      menorFds[x] = $hfsemana[x][y];
    }
    var v1 = subtraiHora(menorFds[x].text(),hAtual);
    var v2 = subtraiHora($hfsemana[x][y].text(),hAtual);

    if(v1 > v2){
      menorFds[x] = $hfsemana[x][y]
    };

  };
};




finalcd = [];
if(fds){
  $('.hsala').each(function(index){
    var hora,minutos;
    hora = menorFds[index].text().substr(0,2);
    minutos = menorFds[index].text().substr(3,2);
    finalcd[index] = new Date();
    finalcd[index].setHours(hora,minutos);
    $(function() {
    var d, h, m, s;
    $('.ctn').eq(index+1).countdown(finalcd[index], function(event) {
      var timeFormat = "%d day(s) %h:%m:%s"
          $this = $(this);
      switch(event.type) {
        case "days":
          d = event.value;
          break;
        case "hours":
          h = event.value;
          break;
        case "minutes":
          m = event.value;
          break;
        case "seconds":
          s = event.value;
          break;
        case "finished":
          $this.fadeTo('slow', 0.5);
          break;
      }
      // Assemble time format
      if(d > 0) {
        timeFormat = timeFormat.replace(/\%d/, d);
        timeFormat = timeFormat.replace(/\(s\)/, Number(d) == 1 ? '' : 's');
      } else {
        timeFormat = timeFormat.replace(/%d day\(s\)/, '');
      }
      timeFormat = timeFormat.replace(/\%h/, h);
      timeFormat = timeFormat.replace(/\%m/, m);
      timeFormat = timeFormat.replace(/\%s/, s);
      // Display
      $this.html(timeFormat);
    });
  });
  //countdown
    menorFds[index].html('<span>'+menorFds[index].text()+'</span>').addClass('active');
  });
}
else{
 $('.hsala').each(function(index){
    var hora,minutos;
    hora = menorDs[index].text().substr(0,2);
    minutos = menorDs[index].text().substr(3,2);
    finalcd[index] = new Date();
    finalcd[index].setHours(hora,minutos);
    $(function() {
    var d, h, m, s;
    $('.ctn').eq(index+1).countdown(finalcd[index], function(event) {
      var timeFormat = "%d day(s) %h:%m:%s"
          $this = $(this);
      switch(event.type) {
        case "days":
          d = event.value;
          break;
        case "hours":
          h = event.value;
          break;
        case "minutes":
          m = event.value;
          break;
        case "seconds":
          s = event.value;
          break;
        case "finished":
          $this.fadeTo('slow', 0.5);
          break;
      }
      // Assemble time format
      if(d > 0) {
        timeFormat = timeFormat.replace(/\%d/, d);
        timeFormat = timeFormat.replace(/\(s\)/, Number(d) == 1 ? '' : 's');
      } else {
        timeFormat = timeFormat.replace(/%d day\(s\)/, '');
      }
      timeFormat = timeFormat.replace(/\%h/, h);
      timeFormat = timeFormat.replace(/\%m/, m);
      timeFormat = timeFormat.replace(/\%s/, s);
      // Display
      $this.html(timeFormat);
    });
  });
  //countdown
    menorDs[index].html('<span>'+menorDs[index].text()+'</span>').addClass('active');
  })
}


</script>
<script src="/assets/jquery.countdown.js" type="text/javascript" charset="utf-8"></script>